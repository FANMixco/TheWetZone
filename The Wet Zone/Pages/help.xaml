<phone:PhoneApplicationPage
    x:Class="The_Wet_Zone.Pages.help"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True" Loaded="PhoneApplicationPage_Loaded">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <!--Pivot Control-->
        <phone:Pivot Title="AYUDA">
            <!--Pivot item one-->
            <phone:PivotItem Header="iconos">
                <Grid>
                    <ScrollViewer>
                        <StackPanel>
                            <phone:LongListSelector x:Name="hList">
                                <phone:LongListSelector.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="{Binding icon}" Height="128" Width="128" Stretch="Fill"/>
                                            <StackPanel>
                                                <TextBlock Text="{Binding title}" FontWeight="Bold" Style="{StaticResource PhoneTextNormalStyle}" FontSize="24"/>
                                                <TextBlock Height="100" TextWrapping="Wrap" Text="{Binding description}" Style="{StaticResource PhoneTextNormalStyle}" Width="auto"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </phone:LongListSelector.ItemTemplate>
                            </phone:LongListSelector>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </phone:PivotItem>
        </phone:Pivot>
    </Grid>
    
</phone:PhoneApplicationPage>